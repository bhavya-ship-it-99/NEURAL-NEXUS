async function loadForecast(){try{await Aero.locate();const d=await Aero.get(`/api/forecast/?lat=${Aero.coords.lat}&lon=${Aero.coords.lon}&days=7`);document.querySelector('#forecastGrid').innerHTML=d.forecast.map(x=>`<div class="forecast-day"><div class="dow">${new Date(x.date+'T12:00:00').toLocaleDateString('en-IN',{weekday:'long'})}</div><div class="date">${new Date(x.date+'T12:00:00').getDate()} ${new Date(x.date+'T12:00:00').toLocaleDateString('en-IN',{month:'short'})}</div><div class="weather-symbol">${Aero.weatherEmoji(x.weather_code)}</div><div class="forecast-temp">${Math.round(x.temp_max)}° / ${Math.round(x.temp_min)}°</div><div class="muted" style="font-size:12px">${Aero.weatherCode(x.weather_code)}</div><div class="forecast-meta"><span>💨 ${Math.round(x.wind_speed_max)} km/h</span><span>💧 ${Math.round(x.humidity_max)}%</span></div></div>`).join('');}catch(e){document.querySelector('#error').textContent=e.message;document.querySelector('#error').classList.remove('hidden')}}loadForecast();
